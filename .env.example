# ── Core ──────────────────────────────────────────────────────────────────
NODE_ENV=development
LOG_LEVEL=info

# Postgres (matches docker-compose.yml defaults)
DATABASE_URL="postgresql://clawd:clawd@localhost:5432/clawd_agents"

# Redis — bridges events between the bot and worker processes (they're
# separate Node processes; this is how the bot learns about trades the
# worker made, to notify the user) and is available for future job queues.
REDIS_URL="redis://localhost:6379"

# ── Telegram ──────────────────────────────────────────────────────────────
# Create a bot with @BotFather on Telegram, copy the token it gives you.
TELEGRAM_BOT_TOKEN=
# Chat the bot DMs platform-level alerts to (circuit-breaker trips, Guard
# accuracy drift) — a group/channel id (negative, e.g. "-1002402221199") or
# a user id. Optional — alerts are just logged if unset.
TELEGRAM_ADMIN_CHAT_ID=

# Bot onboarding — minimum portfolio USD value required to activate a user's
# wallets, and how long the one-time private-key-reveal message at wallet
# creation stays up before auto-deleting.
MIN_DEPOSIT_USD=100
KEY_REVEAL_AUTO_DELETE_SECONDS=300
# Links shown on the bot's "Support" screen — leave blank to show
# "not configured yet" instead of a broken/fake link.
BOT_WEBSITE_URL=
BOT_DOCS_URL=
BOT_CHANNEL_URL=
BOT_SUPPORT_URL=

# ── Wallet custody ────────────────────────────────────────────────────────
# 32-byte (64 hex char) master key used to wrap per-wallet data keys.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# NEVER reuse this value across environments. In production, replace this
# env var with a real KMS (AWS KMS / GCP KMS) — see README "Production hardening".
MASTER_ENCRYPTION_KEY=

# ── Per-chain network selection ──────────────────────────────────────────
# "testnet" (default, safe) or "mainnet" (real funds). Flip only after you've
# verified the chain end-to-end on its testnet/devnet.
SOLANA_NETWORK=testnet
ETHEREUM_NETWORK=testnet
BSC_NETWORK=testnet
BASE_NETWORK=testnet
MONAD_NETWORK=testnet
ROBINHOOD_NETWORK=testnet

# ── Solana ────────────────────────────────────────────────────────────────
# Helius: https://www.helius.dev (free tier has devnet + mainnet RPC + webhooks)
HELIUS_API_KEY=
SOLANA_DEVNET_RPC_URL="https://api.devnet.solana.com"
SOLANA_MAINNET_RPC_URL="https://mainnet.helius-rpc.com/?api-key=REPLACE_ME"
# Comma-separated secondary RPC endpoints — a request that fails against the
# primary above retries against these, in order, before failing the whole
# call (see packages/chains/src/rpc-failover.ts). Optional.
SOLANA_DEVNET_RPC_FALLBACK_URLS=
SOLANA_MAINNET_RPC_FALLBACK_URLS=
# Jupiter swap API is keyless for the public endpoint (rate-limited):
JUPITER_API_BASE="https://quote-api.jup.ag/v6"

# ── EVM (Ethereum, BSC, Base) ─────────────────────────────────────────────
# Alchemy: https://www.alchemy.com  |  QuickNode: https://www.quicknode.com
ALCHEMY_API_KEY=
QUICKNODE_ETHEREUM_URL=
QUICKNODE_BSC_URL=
QUICKNODE_BASE_URL=

ETHEREUM_TESTNET_RPC_URL="https://ethereum-sepolia-rpc.publicnode.com"
ETHEREUM_MAINNET_RPC_URL=
BSC_TESTNET_RPC_URL="https://bsc-testnet-rpc.publicnode.com"
BSC_MAINNET_RPC_URL=
BASE_TESTNET_RPC_URL="https://base-sepolia-rpc.publicnode.com"
BASE_MAINNET_RPC_URL=

# Monad (EVM-compatible L1) — chain 143 mainnet / 10143 testnet.
MONAD_MAINNET_RPC_URL="https://rpc.monad.xyz"
# No default — Monad's testnet RPC is known to get reset/migrated; pull the
# current one from https://docs.monad.xyz right before use if this is stale.
MONAD_TESTNET_RPC_URL="https://testnet-rpc.monad.xyz"

# Robinhood Chain (Arbitrum Orbit L2 for tokenized stocks/RWAs) — chain 4663
# mainnet / 46630 testnet.
ROBINHOOD_MAINNET_RPC_URL="https://rpc.mainnet.chain.robinhood.com"
ROBINHOOD_TESTNET_RPC_URL="https://rpc.testnet.chain.robinhood.com"

# Comma-separated secondary RPC endpoints per chain/network — a request that
# fails against the primary above retries against these, in order, before
# failing the whole call (see packages/chains/src/evm/transport.ts). Optional;
# no failover happens if left empty.
ETHEREUM_TESTNET_RPC_FALLBACK_URLS=
ETHEREUM_MAINNET_RPC_FALLBACK_URLS=
BSC_TESTNET_RPC_FALLBACK_URLS=
BSC_MAINNET_RPC_FALLBACK_URLS=
BASE_TESTNET_RPC_FALLBACK_URLS=
BASE_MAINNET_RPC_FALLBACK_URLS=
MONAD_TESTNET_RPC_FALLBACK_URLS=
MONAD_MAINNET_RPC_FALLBACK_URLS=
ROBINHOOD_TESTNET_RPC_FALLBACK_URLS=
ROBINHOOD_MAINNET_RPC_FALLBACK_URLS=

# Websocket RPC overrides for live subscriptions (new pairs, wallet activity).
# Auto-derived from the http(s) URLs above (scheme swapped to ws/wss) if unset
# — only fill these in if your provider needs a different websocket endpoint.
ETHEREUM_MAINNET_WS_URL=
ETHEREUM_TESTNET_WS_URL=
BSC_MAINNET_WS_URL=
BSC_TESTNET_WS_URL=
BASE_MAINNET_WS_URL=
BASE_TESTNET_WS_URL=
MONAD_MAINNET_WS_URL=
MONAD_TESTNET_WS_URL=
ROBINHOOD_MAINNET_WS_URL=
ROBINHOOD_TESTNET_WS_URL=

# Uniswap-V2-style factory/router/wrapped-native overrides. Ethereum (mainnet)
# and BSC (mainnet) already have working defaults baked in (see
# packages/chains/src/evm/config.ts) — you only need these for testnets, or
# to point Base/Monad/Robinhood Chain at a V2-fork DEX on either network.
# Robinhood Chain in particular is built for tokenized stocks/RWAs, not
# permissionless meme trading, and may not have one at all.
#
# Ethereum -> Sepolia: official Uniswap V2 testnet deployment
# (https://developers.uniswap.org/docs/protocols/v2/deployments).
ETHEREUM_FACTORY_ADDRESS=0xF62c03E08ada871A0bEb309762E260a7a6a880E6
ETHEREUM_ROUTER_ADDRESS=0xeE567Fe1712Faf6149d80dA1E6934E354124CfE3
ETHEREUM_WRAPPED_NATIVE_ADDRESS=0xfFf9976782d46CC05630D1f6eBAb18b2324d6B14
# BSC -> Chapel testnet: official PancakeSwap V2 testnet deployment
# (https://x.com/PancakeSwap/status/1369547285160370182), WBNB testnet
# verified on-chain via testnet.bscscan.com.
BSC_FACTORY_ADDRESS=0x6725F303b657a9451d8BA641348b6761A6CC7a17
BSC_ROUTER_ADDRESS=0xD99D1c33F9fC3444f8101754aBC46c52416550D1
BSC_WRAPPED_NATIVE_ADDRESS=0xae13d989daC2f0dEbFf460aC112a837C89BAa7cd
# Base Sepolia: found a verified V2-style Factory (0x7ae58f10f7849ca6f5fb71b7f45cb416c9204b1e)
# but no matching classic Router02 — Base Sepolia's only confirmed router is
# Uniswap's Universal Router, which has a different call interface
# (execute()-based) this codebase's uniswap-v2.ts doesn't speak. Left blank
# rather than wiring in a mismatched pair.
BASE_FACTORY_ADDRESS=
BASE_ROUTER_ADDRESS=
BASE_WRAPPED_NATIVE_ADDRESS=
# Monad testnet: no verified V2-compatible factory/router found. Uniswap's
# own deployments doc lists a Monad entry, but that address resolves to an
# empty wallet (no bytecode) on Monad's testnet explorer — it's very likely
# the Monad *mainnet* deployment, not testnet. Left blank rather than guessing.
MONAD_FACTORY_ADDRESS=
MONAD_ROUTER_ADDRESS=
MONAD_WRAPPED_NATIVE_ADDRESS=
# Robinhood Chain testnet: no independently verified factory/router pairing
# found (only an unconfirmed entry in Uniswap's deployments doc). Left blank.
ROBINHOOD_FACTORY_ADDRESS=
ROBINHOOD_ROUTER_ADDRESS=
ROBINHOOD_WRAPPED_NATIVE_ADDRESS=

# 1inch: https://portal.1inch.dev — real aggregator integration (mainnet only,
# Ethereum/BSC/Base; falls back to the direct V2 router when unset or on
# testnet, since 1inch has no testnet liquidity).
ONEINCH_API_KEY=

# Flashbots Protect (Ethereum mainnet only) — submits transactions through
# Flashbots instead of the public mempool for frontrunning/revert protection.
# Opt-in.
FLASHBOTS_ENABLED=false
FLASHBOTS_RPC_URL="https://rpc.flashbots.net"

# Jito (Solana mainnet only) — bundles the swap with a tip transaction via
# Jito's Block Engine for MEV-protected/priority inclusion. Opt-in. Verify
# JITO_TIP_ACCOUNTS against https://docs.jito.wtf before relying on this with
# real size — tip accounts do get rotated, and this hasn't been tested
# against a live funded wallet.
JITO_ENABLED=false
JITO_BLOCK_ENGINE_URL="https://mainnet.block-engine.jito.wtf/api/v1/bundles"
JITO_TIP_LAMPORTS=10000
JITO_TIP_ACCOUNTS="96gYZGLnJYVFmbjzopPSU6QiEV5fGqZNyN9nmNhvrZU5,HFqU5x63VTqvQss8hp11i4wVV8bD44PvwucfZ2bU7gRe"

# ── Pricing / pair discovery ──────────────────────────────────────────────
# Birdeye: https://birdeye.so/find-us (Solana + multi-chain token data)
BIRDEYE_API_KEY=
# DexScreener public API is keyless: https://docs.dexscreener.com/api/reference
DEXSCREENER_API_BASE="https://api.dexscreener.com"

# ── Cross-chain (Arbiter) ─────────────────────────────────────────────────
# LiFi: https://li.fi (route + bridge-cost estimation)
LIFI_API_KEY=

# ── Admin: on-chain wallet transaction history ────────────────────────────
# Etherscan's V2 multichain API (one key covers ethereum/bsc/base via a
# chainid param): https://etherscan.io/apis — unset means the admin
# dashboard's user profile page reports EVM transaction history as
# "unsupported" rather than showing a fabricated empty list. Monad and
# Robinhood Chain have no known Etherscan-compatible explorer regardless.
ETHERSCAN_API_KEY=

# ── Fees ──────────────────────────────────────────────────────────────────
# Fee percentage taken on profitable closed trades (0.02 = 2%)
PROFIT_FEE_RATE=0.02
# Treasury address to sweep collected fees to, per chain (optional in dev).
FEE_TREASURY_SOLANA_ADDRESS=
FEE_TREASURY_EVM_ADDRESS=

# ── Router / risk ─────────────────────────────────────────────────────────
# Auto take-profit / stop-loss, as a fraction of entry price (0.5 = +50%, 0.2 = -20%).
TAKE_PROFIT_PCT=0.5
STOP_LOSS_PCT=0.2
# Caps how many open positions Router will hold per chain across all users at once.
MAX_CONCURRENT_POSITIONS_PER_CHAIN=3
# Portfolio-level exposure cap — see packages/specialists/router/src/exposure.ts.
MAX_PORTFOLIO_EXPOSURE_PCT=0.6
# Dynamic (fractional-Kelly) position sizing — see .../router/src/sizing.ts.
KELLY_FRACTION=0.25
MAX_POSITION_SIZE_PCT=0.2
# How often (ms) the position monitor re-checks open positions for TP/SL triggers.
POSITION_MONITOR_INTERVAL_MS=15000

# Circuit breaker (realized-P&L drawdown pause) — see .../router/src/circuit-breaker.ts
# and drawdown-check.ts. Trips the TRADING_PAUSED Setting; clear it from the
# admin dashboard's Settings page.
DRAWDOWN_WINDOW_MS=86400000
DRAWDOWN_THRESHOLD_PCT=0.3
DRAWDOWN_CHECK_INTERVAL_MS=300000

# Guard decision-drift detection — see packages/specialists/guard/src/drift.ts
# and drift-report.ts. Alert threshold / rolling-window size are also tunable
# live via the admin dashboard (Setting overrides).
GUARD_DRIFT_CHECK_INTERVAL_MS=1800000
GUARD_DRIFT_ALERT_THRESHOLD_PCT=0.15
GUARD_DRIFT_ROLLING_WINDOW_SIZE=50
# How often (ms) Arbiter re-scans cross-chain prices for the tracked asset list.
ARBITER_SCAN_INTERVAL_MS=30000
# Placeholder bridging-cost assumption (0.005 = 0.5%) until a live LI.FI quote
# integration replaces it — see packages/specialists/arbiter/src/scan.ts.
ARBITER_ASSUMED_BRIDGE_COST_PCT=0.005
# Minimum spread (after the assumed bridge cost) before Arbiter reports an opportunity.
ARBITER_MIN_SPREAD_PCT=0.01
# Notional size (in the asset's own units, e.g. 1000 USDC) used when
# requesting a real LI.FI quote to confirm the bridge cost on a candidate
# that clears the cheap pre-filter above.
ARBITER_QUOTE_SIZE=1000

# ── AI features ───────────────────────────────────────────────────────────
# Off by default. When enabled: Guard's final qualitative review gate (after
# the mechanical checks pass), Scout's tweet interpretation, and Router's
# periodic TP/SL re-evaluation. Everything works with this off — it just
# means those three steps are skipped/no-op'd rather than the bot failing.
AI_FEATURES_ENABLED=false
# https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5
# How often (ms) Router re-evaluates TP/SL per open position via AI — separate
# from and much slower than the position monitor's per-tick price check.
AI_TP_SL_REVIEW_INTERVAL_MS=300000

# ── KOL / Twitter tracking (Scout) ────────────────────────────────────────
# X API v2 bearer token — the tiers that support monitoring specific
# accounts' posts are paid. https://developer.twitter.com/en/portal/dashboard
TWITTER_BEARER_TOKEN=
# Comma-separated handles (no @) Scout watches for token calls.
KOL_TWITTER_HANDLES=
TWITTER_POLL_INTERVAL_MS=60000

# ── Admin dashboard rule engine ───────────────────────────────────────────
# How often (ms) every active Rule is re-evaluated against every user.
RULE_ENGINE_INTERVAL_MS=60000

# ── Admin dashboard (apps/admin) ──────────────────────────────────────────
# Single-operator login, not a multi-tenant admin system.
ADMIN_USERNAME=
# Generate with (the dummy DATABASE_URL/REDIS_URL are required because
# importing @clawd/wallet transitively loads @clawd/core's config validation,
# even though hashPassphrase itself needs neither):
# DATABASE_URL=x REDIS_URL=x node -e "require('@clawd/wallet').hashPassphrase(process.argv[1]).then(console.log)" "your-password-here"
# (run from the repo root after `npm install` + `npm run build`)
ADMIN_PASSWORD_HASH=
# Random secret used to sign the admin session cookie.
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_SESSION_SECRET=
